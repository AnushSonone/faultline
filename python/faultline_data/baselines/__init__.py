"""Baseline RCA methods run under the same RE2-OB protocol as Faultline.

Kept separate from the converter package: the baselines need RCAEval 1.6.0
installed in `python/.venv-baselines` (see `python/baselines-requirements.txt`),
while the split manifest reader and the freeze preflight are stdlib only.
"""
