# Datasets

- `raw/` — downloaded source data (gitignored)
- `normalized/` — Parquet partitions (large files gitignored; manifests may be committed)
- `fixtures/` — small committed fixtures for tests
- `manifests/` — dataset pins and checksums
