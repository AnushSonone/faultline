# RESULTS

Every number below is reproducible with `scripts/run-benchmarks.sh` and carries the integrity block at the bottom. Claim discipline: percentiles are DDSketch approximations (alpha 0.01); recovery is checkpoint recovery with idempotent incident projections, not exactly-once; the evaluation dataset is SYNTHETIC (generated by `python -m faultline_data.generate_suite --seed 7`), NOT RCAEval - real-data results are pending the RCAEval RE2-OB conversion run.

## Root-cause evaluation (TA-048, synthetic suite)

Untuned spec-18.4 weights, train-free, 16 incidents (4 fault types x 4 target services), labels hidden from the pipeline:

| metric | value |
|---|---|
| top-1 accuracy | 1.000 |
| top-3 accuracy | 1.000 |
| MRR | 1.000 |

Per fault type (n=4 each): cpu top-1 1.00, error top-1 1.00, latency top-1 1.00, mem top-1 1.00.

Ablations (one component removed, top-1): no_change_proximity 1.00, no_critical_path 1.00, no_failed_trace_coverage 1.00, no_log_evidence 1.00, no_temporal_precedence 1.00, no_topology 1.00.

**Honest read:** perfect scores on this suite mean the synthetic faults are cleanly separable, and robustness to single-component ablation shows redundancy across features. This is a smoke test of the pipeline, not evidence of real-world accuracy. Published baselines on RCAEval (the intended real benchmark): best academic baseline ~42% top-1; commercial multi-modal agents ~89% (NOFire AI SRE benchmark page).

## Root-cause evaluation on REAL data (RCAEval RE2-OB, Phase A)

15 real fault-injection cases (5 services x cpu/mem/delay, instance 1) from RCAEval-v2
(Figshare file 60960049, sha256 72006b45..., MIT license), converted by
`python -m faultline_data.adapters.rcaeval` (traces sampled 1/8 by whole trace, logs capped;
sampling recorded per-incident in labels notes). Untuned spec-18.4 weights, labels hidden
from the pipeline:

| metric | value |
|---|---|
| top-1 accuracy | 0.267 |
| top-3 accuracy | 0.467 |
| MRR | 0.407 |

Per fault type (n=5 each): cpu top-1 0.20, delay top-1 0.20, mem top-1 0.40.

Ablations (top-1): no_change_proximity 0.27, no_critical_path 0.27, no_failed_trace_coverage 0.27, no_log_evidence 0.27, no_temporal_precedence 0.13, no_topology 0.07.

**Honest read:** the untuned deterministic pipeline is far weaker on real data than on the
synthetic suite - 26.7% top-1 vs 100%. Ablations show which features actually carry real-data
ranking: topology consistency (remove it: 26.7% -> 6.7%) and temporal precedence (-> 13.3%).
Change proximity, log evidence, failed-trace coverage, and critical-path contribution move
nothing here: RCAEval has no deployment events, and status codes plus sampled traces carry
little error signal for these fault types. Context, not comparison: published RCAEval numbers
(different subsets/protocols) report ~42% top-1 for the best academic baseline and ~89% for a
commercial multi-modal agent. Improving real-data accuracy (metric-name semantics, trace
latency deltas as anomaly input, tuned weights on a training split) is future work and must be
reported against these same untuned numbers.

## Engine benchmarks (TA-049)

200000 rows/run, best of 5 runs, release build. wall-clock only; CPU time and peak memory not instrumented.

| workload | row-baseline rows/s | best batch | rows/s | speedup | p99 batch |
|---|---|---|---|---|---|
| filter_heavy | 2.33e+07 | 16384 | 3.58e+08 | 15.3x | 57 us |
| aggregation_heavy | 2.14e+07 | 4096 | 1.23e+08 | 5.7x | 41 us |
| percentile | 2.68e+07 | 16384 | 1.96e+08 | 7.3x | 100 us |
| temporal_join | 2.74e+06 | 64 | 2.77e+06 | 1.0x | 30 us |

**Honest read:** batching pays off 5-15x for filter/aggregate/percentile paths. The temporal join shows ~1.0x because per-row state mutation dominates; batching does not help it and this is reported as-is.

## Checkpoint + recovery (TA-050)

| metric | value |
|---|---|
| checkpoint size | 22684 bytes |
| checkpoint write p50/p99 | 11.9 / 15.1 ms |
| recovery (read+validate) p50/p99 | 0.42 / 0.76 ms |
| duplicate projections after recovery | 0 |

State sizes are small (fixture-scale session); write latency is fsync-dominated.

## Frontend (TA-051)

| metric | value |
|---|---|
| time to first visual | 98 ms |
| ws live | 136 ms |
| WS frames/s during replay | 36.7 |
| tab switch avg | 28 ms |
| scrubber seek latency | 30 ms |
| JS heap after replay | 20.7 MB |

## Integrity

```
git_commit: d362564bd9c893502b6446cfb73910a4c3f61b99
git_dirty: 102 files
date_utc: 2026-07-28T04:57:48Z
os: Darwin 25.5.0 arm64
rustc: rustc 1.97.1 (8bab26f4f 2026-07-14)
node: v24.5.0
python: Python 3.14.6
machine: Apple M4 Pro, 24 GB
eval_suite_seed: 7
fixture_manifest_sha256: 31fd1cf3b54316d285f7cd32cd75f1f6adc7af1ef116a42548f8bfe7320c00c7
```

Raw outputs: `benchmarks/*.json`. Commands: see `scripts/run-benchmarks.sh`.
