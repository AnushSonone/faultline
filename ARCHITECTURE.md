# Faultline Architecture

Single-process Rust backend (`faultlined`) with internal crates, deterministic replay (`faultline-replay`), CLI (`faultline-cli`), and React web UI.

See `Faultline_Agent_Project_Specification.txt` and brain `PLAN.md` for the full contract.
